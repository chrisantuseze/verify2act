# Few-shot example: two round nuts, mid-episode — first nut already assembled
task: example_2
instruction: "Assemble all round nuts onto their matching pegs."
objects:
  - "front-left round nut"
  - "back-right round nut"
history:
  - "front-left round nut"   # assembled in previous real step

# Images (relative to this file)
current_image: example_2_current.png
goal_image: example_2_goal.png
horizon: 4

# Behaviors
plan:
  - "back-right round nut"
plans:
  - ["back-right round nut"]

# Reflect (not used for this example)
analysis: Null
revised_plan: Null

# OpenAI
role: system
name: Null
name_query: example_user
name_response: example_assistant
