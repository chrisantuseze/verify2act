# Task configuration for robosuite PickPlace task
task: robosuite_pickplace
instruction: "Put all objects in the bin."
objects: 
  - "milk"
  - "cereal"
  - "bread"
  - "can"
  - "bin"
predicates:
  - "On(milk, table)"
  - "On(cereal, table)"
  - "On(bread, table)"
  - "On(can, table)"

# Behaviors
goals: [["On(milk, bin)", "On(cereal, bin)", "On(bread, bin)", "On(can, bin)"]]
plans: [["Pick(milk, table)", "Place(milk, bin)", "Pick(cereal, table)", "Place(cereal, bin)", "Pick(bread, table)", "Place(bread, bin)", "Pick(can, table)", "Place(can, bin)"]]
hypotheses: Null
geometric: Null

# OpenAI
role: system
name: Null
name_query: example_user
name_response: example_assistant
